server:
  port: 8901
spring:
  application:
    name: microservice-consumer-user-feign
eureka:
  client:
    service-url:
     defaultZone: http://localhost:8761/eureka/
  instance:
     prefer-ip-address: true
feign:
   hystrix:
     enabled: true

